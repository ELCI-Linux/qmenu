This directory is used to house _list.sh files.
The qmenu main menu script searches /bin/qmenu/lists for all files ending in the aforementioned string.
To create a new main menu option simply create a shell script that triggers the action(s) you want and name it as {your-script-title}_list.sh
